# Deepwave-Inspired Implementation Change Log

This is the compact active record for this branch. It should stay short.

| Date | Step | Result | Next action |
| --- | --- | --- | --- |
| 2026-06-04 | Created `bv1.2-deepwave-propagator` branch from `bv1.2@4507a08` | dedicated branch for Deepwave-inspired propagator design and implementation | define acoustic operator contract before adding new kernel code |
| 2026-06-04 | Added acoustic operator contract document | clarified ADFWI formula boundary, first pressure-only target, storage-policy boundary, and validation order | implement a separate forward-pressure prototype and compare against production receiver output |
| 2026-06-04 | Built production acoustic FWI baseline matrix | measured 1/3/40 shots with 3/10 FWI iterations on full-record Marmousi2, `checkpoint_segments=10`, `npu:0`; all runs finite | use this as the pre-implementation comparison table for custom operator/storage-policy work |
| 2026-06-04 | Added production checkpoint sweep | measured `checkpoint_segments=1/5/10`; checkpoint=1 is faster but much higher memory, checkpoint=5 is slower and higher memory than checkpoint=10 | target ckpt=1-like speed without accepting its memory increase |
| 2026-06-04 | Normalized baseline archive layout and added test guidelines | moved checkpoint=10 results into `baselines/checkpoint10/` and defined minimal per-stage test gates | use the guidelines to avoid full-matrix reruns during small optimization steps |
| 2026-06-04 | Added per-round optimization workflow and Phase B focus record | fixed the workflow boundary and selected forward parity as the first implementation gate | run tiny parity smoke before deeper custom-operator work |
| 2026-06-04 | Standardized benchmark output paths and ran Phase B tiny parity smoke | prototype output/loss matched production exactly; `v.grad` max abs diff `5.17e-26`; tiny-case speed is directional only | run reduced Marmousi2 one-iteration parity next |
| 2026-06-04 | Ran reduced Marmousi2 one-iteration parity | output/loss match exactly; synthetic-energy raw `vp.grad` max abs diff `3.55e-15`; observed-pressure raw grad is non-finite in both reference and candidate | add observed-pressure gradient diagnostics before full validation |
| 2026-06-04 | Added observed-pressure gradient health diagnostics | production and experimental raw gradients have identical non-finite pattern: 1984 NaN entries and first non-finite index `[1, 0]` | use synthetic-energy for raw-gradient parity; use observed-pressure for output/loss parity until processed-gradient gate is defined |
| 2026-06-04 | Defined and implemented processed-gradient gate | synthetic-energy processed gradients are finite with max abs diff `7.32e-4`; observed-pressure processed gradients are identically non-finite in production and experimental paths | use synthetic-energy for finite gradient parity; keep observed-pressure to output/loss parity before short-loop testing |
| 2026-06-04 | Ran short-loop `3 shot x 3 iter` gate | production and experimental both become non-finite after the first optimizer step; candidate memory ratio is `2.37x`; speedup is not an acceptance result | stop expanding test standards; move to actual algorithm/operator work or stop this Python prototype path |
| 2026-06-04 | Added acoustic operator boundary module | introduced `acoustic_operator.py` contract and tests for future compiled/autograd pressure-only operator; production kernel unchanged | implement a real opt-in compiled prototype behind this boundary, starting with forward receiver parity |
| 2026-06-04 | Added acoustic operator torch-reference backend | `acoustic_pressure_operator(..., backend="torch_reference")` now dispatches to the production pressure-only kernel for exact parity reference; default compiled backend still unavailable | implement the first compiled/autograd forward-pressure backend and compare to the reference backend |
| 2026-06-04 | Added custom-autograd forward prototype | `backend="custom_autograd_forward"` now returns receiver pressure through a custom `torch.autograd.Function` shell and rejects backward explicitly | implement or reject the backward/rematerialization side using tiny finite-gradient parity |
| 2026-06-04 | Added custom-autograd remat gradient prototype | `backend="custom_autograd_remat"` recomputes pressure forward during backward; tiny synthetic-energy `vp.grad` parity vs reference passed with max abs diff `0.0` | run timed forward+backward comparison before deciding whether this Python remat route is worth keeping |
| 2026-06-04 | Benchmarked and bounded Python remat backend | remat is slower (`0.85x`) and higher-memory (`1.02x-1.05x`) for checkpoint=1; checkpoint=10 is incompatible with reentrant checkpoint + `autograd.grad` | stop Python remat as the main route; move to low-level compiled/backend pressure update work |
| 2026-06-04 | Tested custom pressure-update backward route | one-step pressure update custom backward gives mean total speedup `1.29x` at 8 shots and `1.31x` at 40 shots with exact output and near-zero gradient diff | build an opt-in non-scripted pressure-only segment using this custom update before any production integration |
| 2026-06-05 | Tested custom pressure-update inside a multi-step segment | segment parity passed, but total speed is weak: `1.05x` at 3 shots and `0.97x` at 40 shots because snapshot saves are needed around in-place recurrence | stop Python segment integration; move the pressure-update idea into a lower-level compiled/time-segment route |
