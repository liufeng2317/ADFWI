# Archive

This directory keeps detailed experiment history for traceability.

- `round-logs/`: old per-round Markdown notes.
- `results/`: raw JSON benchmark outputs.

These files are not the active optimization plan. Use the top-level summary and
master plan before starting any new performance work.

