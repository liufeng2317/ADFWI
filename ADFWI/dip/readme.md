Some Description of the file:
1. dip_acoustic_fwi.py
   * the same to acoustic_fwi.py while modified the gradient processor
 * dip_elastic_fwi.py
   * the same to elastic_fwi.py while modified the gradient processor
 * dip_acoustic_model.py
   * the model definer: use neural network to represent the vp
 * dip_acoustic_model_vp_rho.py
   * the model definer: use neural network to represent the vp and rho